<h1>New sponsor</h1>

<% form_for(@sponsor) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form_fields', :locals => {:f => f} %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', sponsors_path %>